import 'package:flutter/material.dart';

class PenyakitDetail extends StatelessWidget {
  const PenyakitDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mandi'),
    );
  }
}
